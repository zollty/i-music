buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
    	classpath 'com.android.tools.build:gradle:7.0.3'
    }
}
allprojects {
    repositories {
        google()
        jcenter()
    }
}
ext {
    compileSdkVersion = 31
    targetSdkVersion = 31
    minSdkVersion = 15
}
